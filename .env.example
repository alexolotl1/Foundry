# Supabase project credentials (public/anon key is safe for client use).
# Not wired up yet — the app currently runs entirely on local mock data
# in src/data. Copy this file to .env.local once the real project exists.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
